/target
Cargo.lock
# large downloaded / derived data — keep manifests, not payloads
/database/raw/
/database/refs/
/database/graphs/
*.itpp
*.gfa
*.fa
*.fasta
*.fa.gz
*.fasta.gz
*.bam
*.cram
*.vcf
*.vcf.gz
# but do track small fixtures explicitly committed under experiments/
!experiments/**/*.gfa
!experiments/**/*.fa
# the browser's default dataset IS committed (so the page loads from our own project)
!database/mhc-c4.itpp
# wasm build artifacts (regenerated by gui/build.sh); the assembled HTML is committed
/gui/pkg/
/gui/pkg-node/
/gui/tiles/chr6/
